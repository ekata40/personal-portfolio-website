"use client"; import {useState} from "react"; export function FAQ({items}:{items:{q:string;a:string}[]}){const [open,setOpen]=useState<number|null>(0);return <div className="space-y-3">{items.map((f,i)=><div className="surface rounded-xl" key={f.q}><button className="focus-ring flex w-full items-center justify-between gap-4 rounded-xl px-5 py-4 text-left font-bold" aria-expanded={open===i} onClick={()=>setOpen(open===i?null:i)}>{f.q}<span className="text-cyan">{open===i?"−":"+"}</span></button>{open===i&&<p className="px-5 pb-5 leading-7 text-slate-300">{f.a}</p>}</div>)}</div>}
