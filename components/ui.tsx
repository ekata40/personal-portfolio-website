import Link from "next/link";
import { ReactNode } from "react";
export function Button({href,children,secondary=false}:{href:string;children:ReactNode;secondary?:boolean}){return <Link className={`focus-ring inline-flex items-center justify-center rounded-full px-5 py-3 text-sm font-bold transition ${secondary?"border border-white/15 bg-white/5 hover:bg-white/10":"bg-gradient-to-r from-violet to-cyan text-slate-950 hover:brightness-110"}`} href={href}>{children}</Link>}
export function Heading({eyebrow,title,text}:{eyebrow?:string;title:string;text?:string}){return <div className="max-w-2xl"><p className="eyebrow">{eyebrow}</p><h1 className="mt-3 text-4xl font-bold tracking-tight sm:text-5xl">{title}</h1>{text&&<p className="mt-4 text-lg leading-8 text-slate-300">{text}</p>}</div>}
export function CTA(){return <section className="wrap my-20"><div className="surface overflow-hidden rounded-3xl p-8 sm:p-12"><p className="eyebrow">Make the next move clear</p><h2 className="mt-3 max-w-xl text-3xl font-bold sm:text-4xl">Ready to make your marketing work smarter?</h2><p className="mt-4 max-w-xl text-slate-300">Bring the question, challenge, or ambition. We’ll find a practical starting point together.</p><div className="mt-7"><Button href="/contact">Book a Consultation</Button></div></div></section>}
