import type {MetadataRoute} from "next";import {site} from "@/content/site";import {services} from "@/content/services";import {posts} from "@/content/blog-posts";export default function sitemap():MetadataRoute.Sitemap{return["","/about","/services","/blog","/contact",...services.map(s=>`/services/${s.slug}`),...posts.map(p=>`/blog/${p.slug}`)].map(url=>({url:`${site.url}${url}`,lastModified:new Date()}))}
